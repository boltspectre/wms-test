# WMS 项目测试与验证文档（测试.md）

> 本文件记录每个 plan 功能点的**修改内容**与**测试校验方法**。
>
> 约定：
> - 功能以「完整功能为单位」前后端一起完成（仅单独后端无对应前端的除外）。
> - 每完成一个功能，既在本文件**追加**对应章节，也在对话中**直接返回**同样的测试说明。
> - 前端测试以浏览器实际操作为准；后端以 Swagger / curl 为准。
> - 沙箱运维注意：改完后端代码后需 `taskkill /F /IM python.exe` 清掉残留 uvicorn，再重启唯一干净实例，否则旧进程会返回诡异 500。

---

## 功能一：入库单（必做1）✅ 已完成

### 修改内容
1. 后端 `backend-python/app/routers/inventory.py`
   - 实现 `POST /api/inbound-orders`：生成单号 `IN-YYYYMMDD-XXX`（当日单数+1，唯一冲突重试）、事务内创建入库单 + 明细 + 累加/新建库存、校验商品/库位不存在 → 400。
2. 后端 `backend-python/app/schemas.py`
   - 在 `InboundOrderCreate` / `InboundItemRequest` 加 Pydantic `alias` + `populate_by_name=True`，兼容前端 camelCase（`supplierName` / `productId` / `locationCode`），前端与文档不动。
3. 前端 `frontend-vue/src/views/InboundView.vue`
   - 商品远程搜索下拉（带缓存避免已选项 label 丢失）、仓库→库位级联、数量、多行增删、提交校验与重置。

### 如何测试

#### 后端 API（Swagger 最直观）
- 打开 http://localhost:8000/docs → `POST /api/inbound-orders` → Try it out
- 请求体：
  ```json
  {
    "supplierName": "测试供应商",
    "items": [{"productId": 1, "quantity": 50, "locationCode": "WH-A-01-01"}]
  }
  ```
- 预期：201，data 含 `orderNo`（如 `IN-20260817-001`）与 `items[]`。
- 负向：把 `productId` 改 `999` → 400；把 `locationCode` 改成不存在值 → 400。

#### 后端 API（curl）
```bash
# 合法创建（预期 201，库存 +50）
curl -s -X POST http://localhost:8000/api/inbound-orders \
  -H "Content-Type: application/json" \
  -d '{"supplierName":"测试供应商","items":[{"productId":1,"quantity":50,"locationCode":"WH-A-01-01"}]}'

# 不存在的商品（预期 400）
curl -s -X POST http://localhost:8000/api/inbound-orders \
  -H "Content-Type: application/json" \
  -d '{"supplierName":"x","items":[{"productId":999,"quantity":10,"locationCode":"WH-A-01-01"}]}'

# 不存在的库位（预期 400）
curl -s -X POST http://localhost:8000/api/inbound-orders \
  -H "Content-Type: application/json" \
  -d '{"supplierName":"x","items":[{"productId":1,"quantity":10,"locationCode":"NO-SUCH-LOC"}]}'
```

#### 确认库存真的增加（读库对比）
```bash
cd backend-python
.venv/Scripts/python.exe -c "
import sys; sys.path.insert(0,'.')
from app.database import SessionLocal
from app.models import Inventory
db=SessionLocal()
inv=db.query(Inventory).filter(Inventory.product_id==1, Inventory.location_code=='WH-A-01-01').first()
print('当前库存:', inv.quantity if inv else '无记录')
db.close()
"
```
> 创建前/后各跑一次，数量应 +50。

#### 前端网页操作
1. 打开 http://localhost:5173 → 菜单「入库管理」
2. 填供应商名称 → 「+ 添加明细」
3. 商品框输入关键字（如「耳机」）搜索并选择；选仓库 → 库位下拉自动出现该仓库库位，选一个；填数量
4. 点「提交入库单」→ 绿色提示 `入库单创建成功，单号：IN-...` 并清空表单
5. 负向校验：不填供应商 / 不选商品 / 数量 ≤0 提交 → 对应 warning，不发请求

---

## 功能二：库存查询（必做2）✅ 已完成

### 修改内容
1. 后端 `backend-python/app/routers/inventory.py` 的 `query_inventory`
   - 四表 JOIN（inventory × product × location × warehouse）、`keyword` 模糊（商品名/SKU）、`warehouseId` 过滤、分页（≤100）、camelCase 返回与参数 alias（沿用功能一的 camelCase 约定）。
2. 前端 `frontend-vue/src/views/InventoryView.vue`
   - 搜索栏（300ms 防抖）+ 仓库下拉、表格、库存 <10 红色高亮、后端分页、空结果显示 `el-empty`。

### 如何测试

#### 后端 API（Swagger / curl）
- Swagger：`GET /api/inventory` → Try it out，可选 `keyword` / `warehouseId` / `page` / `pageSize` → 200。
- curl：
  ```bash
  curl -s "http://localhost:8000/api/inventory?page=1&pageSize=2"
  curl -s -G "http://localhost:8000/api/inventory" --data-urlencode "keyword=耳机"
  curl -s -G "http://localhost:8000/api/inventory" --data-urlencode "warehouseId=1"
  ```
- 预期：① 默认分页 `total=7`；② `keyword=耳机` 过滤出蓝牙耳机 Pro；③ `warehouseId=1` `total=6` 且全为「广州主仓」；④ `snake warehouse_id=1` 不被识别（`total=7`，证明前端必须用 camelCase）。

#### 前端网页操作
1. 打开 http://localhost:5173 → 菜单「库存查询」
2. **首屏预期**：7 条库存，列含 商品名称 / SKU / 库位编码 / 仓库 / 库存数量 / 更新时间
3. **红色高亮预期（首屏可见）**：2 行标红 —— `product_id=3 / WH-A-01-02 / 数量=5`、`product_id=4 / WH-A-01-01 / 数量=8`
4. **搜索**：输入框打「耳机」→ 停顿 300ms 自动过滤；清空恢复全部
5. **仓库筛选**：下拉选「广州主仓」→ 只剩该仓库；清空恢复全部
6. **分页**：7 条在第 1 页；超 20 条时翻页重新请求
7. **异常**：后端未启动 → 表格区红色错误提示「加载失败: …」

---

## 功能三：Bug 修复（必做3）✅ 已完成

### 修改内容
1. 后端 `backend-python/app/routers/products.py` 的 `delete_product`
   - 删前查 `Inventory`，有关联库存 → 400 拒绝（含库位与数量）；无库存才删除。原因：SQLite 默认不开 `PRAGMA foreign_keys`，原代码会"成功"却留下孤立库存（预埋 Bug）。
2. 前端 `frontend-vue/src/views/ProductsView.vue` 的 `handleSubmit`
   - 把 `currentPage.value = 1` 移入「新增」分支（`if(!form.value.id) currentPage.value = 1`），编辑时保留当前页码，其余不动。

### 如何测试

#### 后端删除校验（Swagger / curl）
- Swagger：`DELETE /api/products/{product_id}` 试 id=1（有库存，预期 400）、id=5（无库存，预期 200）、id=999（不存在，预期 404）。
- curl：
  ```bash
  curl -s -X DELETE http://localhost:8000/api/products/1   # 有库存 -> 400
  curl -s -X DELETE http://localhost:8000/api/products/5   # 无库存 -> 200
  curl -s -X DELETE http://localhost:8000/api/products/999 # 不存在 -> 404
  ```
- 400 返回：`{"detail":"该商品存在关联库存（库位 WH-A-01-01，数量 …），无法删除。请先处理该商品的库存记录。"}`

#### 前端网页操作
**A. 后端 Bug（删除校验）— 页面可见**
1. 菜单「商品管理」
2. 删除有库存商品（如「蓝牙耳机 Pro」）→ 确认
   - 预期：红色错误提示含「该商品存在关联库存…」，商品仍在列表
3. 删除无库存商品「屏幕保护膜」(id=5) → 确认
   - 预期：绿色「删除成功」，该商品消失

**B. 前端 Bug（编辑跳页）— 需 >10 商品才可见**
- 当前种子仅 6 个商品（pageSize=10，全在第 1 页），此 Bug 页面不可见。复现：在「商品管理」连续新增商品至 >10（出现第 2 页）→ 翻到第 2 页编辑某商品「确定」：
  - 修复前：列表跳回第 1 页
  - 修复后：停留在第 2 页，编辑项原地保留

---

## 功能四：出库单 + 并发安全（选做A）⏳ 待完成
> 完成后在此追加后端（出库单创建 API / 并发安全：BEGIN IMMEDIATE / 乐观锁 / 唯一约束重试）+ 前端（出库单页面）测试说明。

## 选做 B：单元测试 ⏳ 待完成
## 选做 C：前端性能 ⏳ 待完成
## 收尾：NOTES.md（第⑩项）⏳ 待完成
